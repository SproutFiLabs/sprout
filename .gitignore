node_modules/
tmp/
dist/
cache/
.env
.env.*
!.env.example
data/
contracts/out/
contracts/cache/
contracts/broadcast/
*.log


# Local-only generated material
output/
.DS_Store
*.sqlite*
*.pem
*.key

# A global `token*` ignore rule drops OpenZeppelin's token/ sources, which the
# contracts import. Keep them tracked so a fresh clone builds.
!contracts/lib/openzeppelin-contracts/token/

# Download directly from Fontshare for local serving; see the bundled FFL.
web/public/fonts/satoshi/*.woff2
web/public/fonts/satoshi/*.tmp
