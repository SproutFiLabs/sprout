{
  "name": "@sprout/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "dev": "bun ../scripts/ensure-fonts.mjs && bun ../scripts/check-zk.mjs && vite",
    "build": "bun ../scripts/ensure-fonts.mjs && bun ../scripts/check-zk.mjs && tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@sprout/shared": "workspace:*",
    "lucide-react": "1.45.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "snarkjs": "0.7.6",
    "viem": "^2.21.40"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  }
}
